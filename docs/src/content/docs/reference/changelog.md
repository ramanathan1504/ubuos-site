---
title: Changelog
description: What changed in each release, and why.
---

<!-- Generated by the Sync release workflow from published releases.
     Hand edits are overwritten. Write release notes on the release. -->

Current release: **1.8.3**.

## 1.8.3

_2026-08-13_


_2026-08-13_

- Packages runs after Distributions, ships an unversioned deb, and release.sh survives branch protection (#46)
- Bump com.fasterxml.jackson.core:jackson-databind from 2.20.0 to 2.21.5 (#11)
- ci: bump actions/setup-java from 4 to 5 (#13)
- deps: bump the routine group with 6 updates (#14)
- ci: bump actions/checkout from 4 to 7 (#15)
- ci: bump actions/upload-artifact from 4 to 7 (#16)




## 1.8.2

_2026-08-13_


_2026-08-13_

- alias: refuse shell keywords and builtins, which no shim can ever be (#44)




## 1.8.1

_2026-08-12_

One fix: `oss run` without `--pack` now means the pack you are standing in — `cd my-pack && oss run list`, exactly as the connect guide says. The 1.8.0 binary demanded the flag, so the documented short form errored.

## 1.8.0

_2026-08-12_

The release where `oss` becomes complete on its own — and your repository becomes a pack.

### The rule this release implements
**`oss` ships every capability. An extension supplies only what `oss` cannot know: the tools on your machine, and how your project runs.** A fresh install searches, reads pull requests, follows them, remembers, and runs your project — with nothing else installed.

### New
- **`oss run --pack <dir>`** — the matrix engine now ships inside `oss`. Point it at any directory with a `pack.sh`: your applications, configurations, versions. Nothing is registered. [log4j2-workout](https://github.com/ramanathan1504/log4j2-workout) is the worked example — 19 real applications, and its whole contract is 88 lines.
- **`oss followup`** — the union of both halves that had forked: `--since` (what the author pushed since your review, merges correctly excluded), `--write`, `--comment`, `--changed`, `--mine`, `--record`.
- **`oss hub`** — is anyone waiting on you? Reads only; no send path exists.
- **`oss memory`** — built-in: file, index and search notes with nothing attached. An archive extension still takes over when present, and a working copy stays local so your notes keep improving `pick`.
- **`oss pick`** — what to work on next, scored against what you have already reviewed and written. Reviews count 4× — reviewing something is the one place you demonstrably read the code. Says *why* each suggestion matched.
- **`oss backlog`** — the whole backlog as one page: clusters, mergeable now, one fix away.
- **`oss model`** — the optional 22 MB local model that upgrades search from shared words to shared meaning. Fetched only when you ask.
- **`oss backup` / `restore`** — now covers everything that cannot be re-derived: reviews, memory, vectors, the database, and any attached extension's declared archive. `--to <dir>` targets a synced folder. Verified: 1,152 files out, restored whole onto an empty machine, `pick` and `followup` identical afterwards.

### Fixed
- Backup previously archived only the database — your reviews and notes were silently excluded, and one unreadable iCloud file could abort the walk leaving a partial zip that looked real. Both fixed; skipped files are named, partials are never left behind.
- Every stale `oss-cli <verb>` instruction retired for good.

### Docs
[ubuos.com/docs/connect](https://ubuos.com/docs/connect/) — pack or extension, settled in ten seconds, both halves copyable.

## 1.7.2

_2026-08-12_

**The installed command now starts on macOS 26.** In 1.7.0 and 1.7.1 the bundled runtime crashed at JVM init on macOS 26.5 — every command, immediately. The runtime it ships is now built on JDK 21; what you run is unchanged.

If you hit that crash, `brew upgrade oss` is enough.

### Fixed
- **macOS 26 crash.** The bundled Temurin 17.0.19 runtime died with `SIGSEGV at pc=0` four milliseconds in, before any Java frame. Not the launcher, not jlink, not the jar — that JDK build; a jlink 21 image of the same jar runs fine. The runtime is now 21.
- **The release smoke test could not have caught it**, because it runs on the machine that built the image. That limitation is now written into the workflow rather than rediscovered.
- **Instructions that named a command that no longer exists.** After the rename to `oss`, printed hints still said `oss-cli …` — including the one shown when an extension goes stale. Copying one got `command not found`. `bench` and `kb` in those strings are now `run` and `memory`.

## 1.7.1

_2026-08-12_


_2026-08-12_

- Resolve relative symlinks against the link, not the working directory (#33)
- Point at the runner from the place the question arises (#32)




## 1.7.0

_2026-08-12_


_2026-08-12_

- Stop one unavailable runner blocking the whole release (#31)
- jlink: --compress=2, not zip-6 (#30)
- Apply the formatter, and stop merging red (#29)
- Ship a self-contained oss for every platform (#28)
- Embeddings that run inside the process, with no server (#27)
- Keep the service running on Linux and Windows too, not only macOS (#26)
- Search your own data with no model at all (#25)
- search needs Ollama; the table said it did not (#24)
- Move pr and issue into the core too (#23)
- Move followup into the core, where it never needed a bench (#22)
- Stop bumping the tap from the release workflow (#21)




## 1.6.1

_2026-08-11_


_2026-08-11_

- Call it oss (#20)
- Let people name this command themselves (#19)
- Drop the local docs pane; documentation belongs on the site (#18)
- deps: bump com.diffplug.spotless:spotless-maven-plugin from 2.43.0 to 3.9.0 (#17)
- Apache 2.0 headers on every source file, contributing rules, dependency alerts (#12)




## 1.6.0

_2026-08-11_


_2026-08-11_

- Call them runners and memories, not benches and kbs
- Offer what is worth doing next, where the last thing finished
- Show what a command is doing while it does it
- Serve: keep it running if asked, and read attached extensions' docs
- Add an installation guide, and say where everything ends up




## 1.5.1

_2026-08-11_


_2026-08-11_

- Detect a drifted manifest instead of silently acting on the snapshot
- Correct two comments the approval redesign left stale




## 1.5.0

_2026-08-11_


_2026-08-11_

- Reframe serve: the page is not the point, the open set of benches is
- Serve on :1504 with a palette you attach benches to
- Upstream writes: refused by default, approved by name, confirmed every time
- Extensions: a bench that runs, a kb that remembers, gated when they write




## 1.4.1

_2026-08-10_


_2026-08-10_

- SETUP: align the markdown tables
- README: say where OSS-CLI sits, of the three
- Publish the site from CI (#10)




## 1.4.0

_2026-08-02_


_2026-08-02_

- Make the release publish step repeatable (#9)
- Build, verify and smoke-test on every push (#8)
- Detect the toolchain Gradle projects declare (#7)
- Add onboard, the contributor's view of a repository profile (#6)
- Cut a release with one command (#5)
- Escalate a review when the diff is larger than the local model can read (#4)
- Add the single-page site, deployable to Cloudflare Pages (#3)
- Let review consult the reviewer's own notes (#2)
- Keep generated notes out of the folders holding hand-written ones (#1)
- Report what actually happened, index every repo, and close the loop
- Remove a merge-conflict marker left in .gitignore




## 1.3.1

_2026-08-01_

**Full Changelog**: https://github.com/ramanathan1504/oss-cli/compare/v1.3.0...v1.3.1

## 1.3.0

_2026-08-01_

**Full Changelog**: https://github.com/ramanathan1504/oss-cli/commits/v1.3.0

