---
name: "log4j runner"
tagline: "Does it actually run?"
summary: "Nineteen real applications across a version × config × app matrix, on real JVMs. Built to answer whether an issue reproduces — and to hand back a standalone reproduction you can attach to it."
repo: "https://github.com/ramanathan1504/log4j2-workout"
order: 20
status: "stable"
---

Attaches to `oss` as a runner. The engine is generic; what it tests is a pack you
can swap.
