---
name: "log4j pack"
tagline: "A sample pack, so yours has something to copy."
summary: "Nineteen real applications, seventy-three configurations, seven Log4j versions. It is what the PR extension runs against by default — and the worked example for writing a pack of your own."
repo: "https://github.com/ramanathan1504/log4j2-workout"
order: 20
status: stable
---

The engine is generic. `packs/log4j/` is what makes it about Log4j, and
`packs/example/` is there to be copied.
